name: renaissance_man
description: A web app for tracking progression toward skill mastery.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.6
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  animations:
  flutter_material_pickers:
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito:
  build_runner:
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
